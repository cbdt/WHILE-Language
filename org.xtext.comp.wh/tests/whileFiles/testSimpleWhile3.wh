// Fonction qui renvoie le même nombre qu'en entrée
// Input 5
// Output 25

function main:
read A
%
	while A do
	    while A do
	    	A := (tl A)
	    	Z := (cons nil Z)
	    od
	od

%
write Z